[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.111.1"
uvicorn = {extras = ["standart"], version = "^0.30.6"}
pydantic = "^2.8.2"
pydantic-settings = "^2.4.0"